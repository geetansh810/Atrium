import { PagePlaceholder } from "../PagePlaceholder";

export function KnowledgePage() {
  return (
    <PagePlaceholder
      title="Knowledge"
      subtitle="Arriving in MF-5"
      description="Docs/embeddings schema already exists in core-api, but no endpoints are wired up yet — this page will run on sample data until then."
    />
  );
}
