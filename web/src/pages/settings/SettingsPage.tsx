import { PagePlaceholder } from "../PagePlaceholder";

export function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      subtitle="Arriving in MF-6"
      description="Workspace info, model catalog, and permissions land with the command-palette milestone."
    />
  );
}
