import { PagePlaceholder } from "../PagePlaceholder";

export function ProjectsPage() {
  return (
    <PagePlaceholder
      title="Projects"
      subtitle="Arriving in MF-5"
      description="Projects are a net-new concept with no backend equivalent yet — this page will run on sample data until a real Projects API exists."
    />
  );
}
