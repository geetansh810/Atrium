# web-dashboard

**What:** React dashboard in `web/` — THE UI (the only one, since the office removal). Redesigned from "office-canvas-front-and-center with overlay panels" into a Linear/Notion/Datadog-style "AI Operating System" per the approved plan at `/Users/geetanshagrawal/.claude/plans/multi-agent-employee-platform-redesign-fluttering-crescent.md` (MF-1…MF-6, one milestone per session, run via `/milestone`) — **the full MF series is complete**. The SkyOffice office view survived the MF series as one optional `/office` route but was **removed outright 2026-07-15 (session 23)** — see [[web-office]]; its replacement is the live Team view below.

**State (2026-07-16, M3.1): real Login/Signup gates the whole dashboard in API mode.** New `shared/auth.ts` (localStorage session — token/companyId/userId/displayName/role — reactive via the `createDomainStore` primitive MF-5/MF-6 already built) + `pages/auth/AuthPage.tsx` (single page, Sign-up/Log-in toggle, company-name→slug auto-derive, backend `fieldErrors` shown inline). `App.tsx` gained an `AuthGate`: `VITE_USE_MOCKS=1` skips it entirely (unaffected — mock mode goes straight to the dashboard as before); API mode renders `AuthPage` until a session exists, then mounts the real `AppProvider`/`AppShell` — session persists across a hard refresh, a fresh browser genuinely lands on signup first. `shared/api.ts` rewritten to send `Authorization: Bearer` from the stored session instead of an `X-Company-Id` per-call option; `DEV_COMPANY_ID`/`VITE_COMPANY_ID` retired everywhere (every page that read it — `ReviewInbox`, `ReportsPage`, `EmployeeProfile`, `OrganizationPage`, `SettingsPage` — switched to `useAuthSession()?.companyId`). New "Log out" control + "Signed in as" row on Settings' Company card. `web/scripts/seed-dev.mjs` signs up instead of the old headerless `POST /companies`, prints login credentials instead of writing an env var. **One real pre-existing bug fixed in passing:** `ui/Toast.tsx`'s welcome greeting hardcoded the mock `currentUser.displayName` regardless of mode — now reads the real session's name in API mode. **Environment discovery, unblocks future frontend verification:** the Node 20.9.0 tooling gap (sessions 26–28) isn't a dead end — a working Node 23.10.0 already exists at `/opt/homebrew/bin/node` on this box, just not first on PATH; prefixing `PATH="/opt/homebrew/bin:$PATH"` fixes `npm run dev`/`build`/`lint` completely. **Done-when verified LIVE in an actual browser** using that fix: fresh signup → straight to Mission Control at 0/0 agents (zero prior state); old `X-Company-Id` header alone now 401s, the real bearer 200s (confirmed via network-request inspection); logout → signup/login screen; login with the same credentials → same company restored; hired a real agent through the UI, appeared on the roster — the full authenticated write path, not just reads. Mock mode reconfirmed fully unaffected on a separate throwaway server. Console clean both modes; `tsc`/`lint` clean (9 pre-existing warnings, zero new).

**State (2026-07-16, M-LN2): Review Inbox gained a Memories tab — the last piece of the M-SK/CTX/MEM/LN/KN series' UI surface.** Frontend-only (`git diff core-api/` empty — every backend endpoint it calls, `GET /companies/{id}/memories/review-queue` + `POST /memories/{id}/review`, already existed since M-LN1). `ReviewInbox.tsx` gained `ui/Tabs` (`Tasks (n)` / `Memories`) — the existing pending_review+flagged task queue moved unchanged into the Tasks tab; new `MockMemoriesTab`/`ApiMemoriesTab` pair (`USE_MOCKS ? A : B` at module scope, the same precedent [[milestones]] M2.6/SettingsPage's MF-6 card set) backs the new tab — mock mode shows the same honest "live-API-only, no mock fixture" `EmptyState` M2.6's Memory tab established, since pending memories never had a fixture either. New `queries.ts` hooks `useMemoryReviewQueue(companyId)` (4s poll) + `useReviewMemoryMutation(companyId)` (invalidates both `memory-review-queue` and `memories` query keys so an approved/rejected item also drops out of any open `EmployeeProfile` Memory tab); new `api.ts` `memoryReviewQueue`/`reviewMemory` fetchers. Each pending memory card shows its scope (`agent`→ the agent's name, `role`→ roleKey, `company`→ literal "company"), kind chip, and Approve/Reject buttons calling `POST /memories/{id}/review {action}` — `promoteScope`/`asSkill` (16 §3's optional add-ons) were deliberately left out of this pass; nothing in the Done-when needs them and the two-button loop is enough to close it. **Done-when verified live in mock mode** (throwaway `:5199` server): Tasks tab unchanged (3 real fixture items), Memories tab showed the honest EmptyState. **API mode: endpoint contract verified via `curl` against the real Agrawal Namkeen tenant** (port 5173 occupied by another session, the still-unfixed CORS single-origin gap — same fallback MF-3/M2.6 already set) — `GET .../memories/review-queue` returns `{"data":[]}` cleanly; `POST /memories/{bogus-id}/review` 404s exactly as `api.ts`'s error path expects. **Could not exercise the full reject→lesson→pending→approve loop live**: `LearningPipeline.ingest()` calls `embeddingClient.embed()` unconditionally (no `isReady()` guard, unlike `recall()`'s two call sites) and this environment has no `OPENAI_API_KEY` configured (same pre-existing gap M-MEM1/M-LN1 sessions documented) — a real task was created, completed by Priya, and rejected with brand-name feedback to trigger extraction, but the review-queue stayed empty afterward, confirming no memory has ever actually been ingested in this tenant's history. Flagged as a real latent gap (`LearningPipeline` extraction has never run end-to-end in this project) rather than routed around with a manual DB insert. `tsc`/lint clean (same 9 pre-existing warnings, zero new), zero hardcoded hex, `make check` exit 0.

**Earlier (2026-07-16, M2.6): Agent Profile's Activity/Memory/Performance tabs now real; Workspace/task detail already complete since MF-3.** `pages/employees/EmployeeProfile.tsx` gained a new **Activity** tab (`selectors.synthesizeFeed` scoped to just this agent's own tasks, rendered via `ui/Feed`) — the milestone card's literal "activity feed from task_events" ask. **Performance tab's zeros were stale, not honest** — `apiStore.tsx` hardcoded `agentStats` to all-zero with a comment blaming "M2.3's rollups", but M2.3 shipped session 22; fixed by wiring real `useAgentPerformance(companyId)` data in instead (per-agent `tasksCompleted`/`successRate`; `focusMinutes` stays 0, genuinely untracked — same honesty precedent as [[milestones]] M2.3's Reports page). **Memory tab was a stale "pending M-MEM1" stub** despite M-MEM1/M-LN1 both shipping sessions ago — replaced with a real `MockMemoryTab`/`ApiMemoryTab` pair (`USE_MOCKS ? A : B` at module scope, `SettingsPage`'s MF-6 precedent) backed by new `useAgentMemories(companyId, agentId)` → `GET /companies/{id}/memories?agentId=` (16 §3); mock mode shows an honest "live-API-only, no mock fixture" EmptyState since memories never had one. **Zero backend touched** — `git diff core-api/` empty, matching the prior session's "likely frontend-only" prediction; the Workspace/task-detail half of the card (subtask checklist, output, timeline, conversation) was already fully built by MF-3's `TaskDrawer.tsx`, so nothing there needed changes. **Done-when verified live in mock mode** (throwaway `:5199` server): Activity tab showed CoderAgent's real created→claimed→progress→completed chain across its 2 fixture tasks, newest first; Memory tab showed the honest live-API-only EmptyState; Performance tab unchanged (mock fixture stats). **API mode verified via direct `curl` against the real Agrawal Namkeen tenant** (port 5173 was occupied by another session — the still-unfixed CORS single-origin gap meant a second dev-server port couldn't reach core-api, so this session fell back to curl matching the frontend's exact DTO shapes, same precedent MF-3 set): `GET /analytics/agent-performance` returned Priya at 11 tasksCompleted/100% success (would have rendered as 0/0% before this fix); `GET /memories?agentId=<priya>` returned an honestly-empty list (she has no rejections yet to have triggered lesson extraction). `tsc`/lint clean (same 9 pre-existing fast-refresh warnings, zero new), zero hardcoded hex, `make check` exit 0.

**Earlier (2026-07-16, M2.5): chat + announcements now read the real API (were mock-only in both modes).** `apiStore.tsx` rewritten — channels/messages/announcements come from React Query (`useChannels`/`useChannelMessages`/`useAnnouncements`, 4s poll; only the activity feed stays local); `sendMessage`/`addAnnouncement` dispatch to real mutations (`POST /channels/{id}/messages`, `POST /companies/{id}/announcements`). The stored `activeChannelId` (mock default `"ch-general"`) falls back to the first real channel so `ChatPanel` always opens on something valid, and only the active channel's history is loaded (ConversationThread filters by channelId anyway). `ConversationThread.senderName` now handles a bare `"user"` sender (API mode, no X-User-Id) besides `"user:<id>"`. The [[communication]] `ChatNoticePipeline` makes a completed task post "🤖 {agent} finished '{title}'…" to `#general` — verified live rendering in the actual ChatPanel. `ReviewInbox` is the ≤2-click escalations surface (unchanged, already worked in both modes). **Hidden-tab gotcha:** React Query's `refetchInterval` does not poll a backgrounded tab (`document.hidden`) — the chat looked empty in the automated browser until a forced `invalidateQueries` proved the wiring correct (query/adapter/state all fine; same class as the Phaser-hidden-tab quirk).

**Earlier (2026-07-15, session 23): live Team view replaces the office.** New `pages/team/TeamPage.tsx` + `.css` at `/team` (nav "Team", `/office` redirects there; `TeamIcon` = the old unused two-person `PeopleIcon` glyph, `OfficeIcon` + 7 dead office-room icons deleted). Zones — Working now / Awaiting review / In focus / Idle / Paused / Offline — come from new `selectors.liveTeamZones(agents, tasks)`, a pure derivation over live task assignments (paused → has claimed/in_progress task → has pending_review/flagged task → in_focus → offline → idle, first match wins), NOT the stale `agent.status` field (which nothing on the backend auto-updates); the card's "claimed/completed Nm ago" time comes only from real task events (`sinceIso` is null for status-derived zones — API mode fakes `statusSince`, rendering it would fabricate data). Cards: Avatar/name/role, derived-zone `StatusPill`, clickable current-task row (title + `ProgressBar` + time → `nav.openTask`), skill chips, workload count (new shared `selectors.agentWorkload`, promoted out of `EmployeesPage`'s inline loop — both pages now share it); card click → `nav.openAgent`. `TeamPage` is `ui/PageShell`'s first real adoption (clears that MF-2 carry-forward). Working/Awaiting-review zones always render (muted hint when empty); status zones hide when empty. Works identically in both `VITE_USE_MOCKS` modes by construction — zero new store actions/endpoints (now a hard rule: Team view is pure derivation). **Also removed with the office:** `web/src/office/` (21 files), `pages/office/`, all 1.7MB of `web/public/assets/` (LimeZu launch blocker void), `phaser` dep, `UiState.activeRoom` + `setRoom` action, `Agent.locationKey` (never had a real DB column — `office_layout` was never built), `--status-blocked` token (zero readers), the Makefile's office-realtime stanzas.

**State (2026-07-15, M2.3): Reports' Agent Performance leaderboard + real 7d/top-skills, Organization's period-scoped Budget Ledger.** Not an MF-series milestone — a backend-track card ([[milestones]] M2.3) that also needed frontend wiring since the mock-shaped UI already existed from MF-5. `ReportsPage.tsx`: new `useMockAnalytics`/`useRealAnalytics` hook-pair (picked once via `USE_MOCKS ? A : B` at module scope, `SettingsPage`'s MF-6 precedent) replaces the always-mock `tasks7d`/`topSkills` imports with real `GET /analytics/{tasks-7d,top-skills,agent-performance,summary}` calls in API mode; new Agent Performance leaderboard section (reused `.perf-row` styling from the existing Budget Burn section); an honest "Focus Time isn't tracked yet" caption replaces a fabricated number in API mode (no presence source exists until M2.4c) instead of showing a stale zero. `OrganizationPage.tsx`'s Payroll tab: new `<input type="month">` period selector + `MockCostPerTaskSection`/`ApiCostPerTaskSection` split — the real query (`GET /analytics/cost-per-task?period=`) genuinely re-fetches per period and shows an honest empty state for months with no spend; the mock fixture stays static with a "doesn't vary by period" note. New `shared/format.ts` `formatCost()` — real per-task costs at this demo's usage scale are sub-cent, and the old `costUsd.toFixed(2)` was silently showing "$0.00" for every real row. **Done-when verified live** against the real Agrawal Namkeen tenant (another session's already-running `:5173` server, same `web/` files via Vite HMR): Reports showed real 100% Avg Success Rate / Priya at 100% in the leaderboard / "content 100%" in Top Skills / 3 completions in today's 7d bar, zero "Sample data" pills; Organization's ledger showed real per-task costs with correct precision (`$0.0002` etc.) and the period selector correctly switched to an empty state for a month with no spend. `tsc`/lint clean (same 9 pre-existing fast-refresh warnings, zero new), zero hardcoded hex.

**Earlier (2026-07-14, MF-6): Notifications, Cmd+K, Settings, contract cleanup — series complete.** New `pages/notifications/NotificationsPage.tsx` (derived feed: `shared/notifications.ts`'s `buildNotifications` merges `selectors.synthesizeFeed`'s task events with `state.announcements`; composer ported from the deleted `AnnouncementsPanel`) and `ui/CommandPalette.tsx` (Cmd+K: substring search across Pages/Agents/Tasks/Actions; New Task/Hire Agent/Pause All — the last mirrors M0.8's `toggleSubtask` precedent, real in mock mode, toast-explained no-op in API mode since no bulk-pause endpoint exists) and `pages/settings/SettingsPage.tsx` (Company/Model-Catalog/Data-Source cards, each a `Mock*`/`Api*` component pair picked once via `USE_MOCKS ? A : B` at module scope, matching `store.tsx`'s own provider-swap convention — avoids a real rules-of-hooks violation a naive single-component branch would have hit). **Real bug found live, not by inspection:** `GlobalHeader`'s bell and `NotificationsPage` each calling `useNotifications()` with a local `useState` gave each its own copy of the read-id set, so the badge never cleared after visiting the page — fixed by rebuilding the read-tracking on MF-5's `shared/domains/createDomainStore.ts` external store instead, so every caller shares one source of truth. **Contract cleanup went further than "shrink `PanelKey`," removed it entirely:** `PanelKey`/`openPanel`/`closePanel`/`selectedAgentId`/`selectedTaskId`/`railVisible`/`toggleRail` all deleted; replaced by `chatOpen: boolean` + `setChatOpen`, since Chat is now a **permanent AppShell-level drawer** (mounted unconditionally, gated on `state.ui.chatOpen`, like the modals already were) instead of routed through the panel-key pattern. **Deleted:** `AnnouncementsPanel.tsx`, `PanelShell.tsx`, `shell/LegacyPanelHost.tsx`, `NavSidebar`'s "Classic Panels" section entirely (Chat already had a persistent header icon, so a 1-item list added nothing). `panels.css` (355 lines) deleted outright, content redistributed by actual ownership: true global atoms into `index.css`; `.chat-msg*`/`.chat-input*`/`.chat-icon-btn` — discovered to be owned by the shared `ui/ConversationThread.tsx`, not `ChatPanel` — into `ConversationThread.css`; real `ChatPanel`-only layout classes into new `ChatPanel.css`; `.ann-*` deleted outright. **a11y pass:** `ui/Drawer.tsx` gained `role="dialog"`, a real Tab focus trap, Escape-to-close, focus-restore on close (verified live); `ui/Tabs.tsx` gained `role="tabpanel"` + arrow-key navigation; `CommandPalette` matches the same pattern. **Done-when verified live in both modes** (API mode: this session's own `:5173` server against the real seeded company; mock mode: throwaway `:5181` server): Cmd+K found real agent/task by substring and navigated; bell badge matched derived unread count and cleared correctly post-fix in both modes; posted a real announcement via the composer; Settings showed live company info + live 4-model catalog in API mode and honest mock-mode equivalents otherwise; Pause All genuinely paused all 6 mock agents. `grep -rn "openPanel"` and hardcoded-hex greps both zero matches. `tsc`/lint clean (7 pre-existing + 2 new same-category fast-refresh warnings). `make check` exit 0.

**Earlier (2026-07-14, MF-5): Projects, Knowledge, Workflow, Reports live.** Three net-new mock-only domains (`shared/domains/{projects,knowledge,workflows}.ts` + fixtures) — none touch `AppState`/`Action`, per the plan's mock/API-parity rule. `projects.ts` needed real cross-page shared state (`ProjectsPage` create → `ProjectDetail` must see it after navigating), which plain per-component `useState` can't give, so it's backed by a small hand-rolled external store (`shared/domains/createDomainStore.ts`, `useSyncExternalStore`) instead. Real `pages/projects/{ProjectsPage,ProjectDetail}.tsx` — overview/tasks/artifacts/agents/timeline/budget tabs; Tasks tab reuses `ui/Kanban` filtered to the project's linked *real* task ids (client-side-only mapping, add/remove control); Timeline reuses `selectors.synthesizeFeed` + newly-shared `TASK_EVENT_LABEL`/`taskEventTone` (hoisted out of `TaskDrawer.tsx` so both consumers use one map). `pages/knowledge/KnowledgePage.tsx` — search-filtered doc list + `ui/Drawer` detail. `pages/workflow/WorkflowPage.tsx` — port+upgrade of `TaskFlowPanel`: same real parent→child task chain, now rendered via `ui/Graph` (its first non-mini, non-org-chart real DAG consumer, live node status colors), plus a separate mock "Workflow Library" section (named templates, distinct from the live instance view). `pages/reports/ReportsPage.tsx` — port of `AnalyticsPanel` + three new real-derived sections (status distribution, per-agent budget burn, completion counts) alongside the pre-existing always-mock trend/skills charts. Mock-only sections show a "Sample data" `StatusPill` in API mode only (`!USE_MOCKS`) — never in mock mode, where everything is already fixture-based. **Deleted:** `TaskFlowPanel.tsx`, `AnalyticsPanel.tsx`; `PanelKey` shrank to 2 (`chat`/`announcements` — only `openChat` still routes through a legacy panel, pending MF-6). **Third `panels.css` cut:** `.status-badge*`/`.flow-*` removed outright (no successor since `WorkflowPage` uses `ui/Graph` instead of hand-rolled flow nodes); `.kpi-grid`/`.bar-*`/`.perf-*` ported into `ReportsPage.css`; the legacy `.stat-card*` block removed too — it was a genuine MF-2-era CSS collision with `ui/StatCard.css` (same class names, different rules), silently resolved by AnalyticsPanel's deletion since it was the only other reader. **Found and fixed a second real pre-existing bug in passing:** `OrganizationPage.tsx`'s Payroll tab (`.budget-row`/`.budget-cap-btn`/`.cost-row`/`.mono` etc.) had zero matching CSS anywhere despite MF-4's session notes claiming it was ported — added the missing rules to `OrganizationPage.css`; `ProjectDetail`'s own budget-cap editor uses distinct `.project-detail-cap-*` classes rather than depending on those (the established "port, don't share" rule from MF-3). **Done-when verified live in both modes:** API mode (this session's own `:5173` server) — linked a real seeded task ("Reverse a string", GeminiCoder's actual completed output) into a mock project via the Tasks tab's add-task selector, saw it land in the Done kanban column with its real artifact, full audit-chain timeline, and correct agent in the Agents tab; edited the project's budget cap and confirmed it stuck (via tab-switch/blur-save — Enter-to-save also works but this browser-automation harness doesn't always deliver the keydown reliably, a tooling quirk not an app bug); "Sample data" pill correctly present on Projects/Knowledge/Workflow-Library/Reports-trends. Mock mode (throwaway `:5180` server) — pre-seeded project→task links rendered a fully populated board/artifacts/agents/timeline/budget across all six ProjectDetail tabs for all three fixture projects; Workflow page's graph showed correct live node colors (blue/running, green/completed) for a real multi-step mock task chain; no "Sample data" pill anywhere. Console clean both modes. `tsc`/lint clean (same 7 pre-existing fast-refresh warnings, zero new). `grep -rn "#[0-9a-fA-F]\{3,6\}"` zero matches across every new/touched file.

**Addendum, same session: stalled-task alert.** User hit a real gap — creating a task via the header modal looked like it did nothing. Actual cause: this company's `core-api` has no `ANTHROPIC_API_KEY`, so all 3 Anthropic agents sit permanently parked (only `GeminiCoder`/Google can claim anything); the task was created fine but aged unassigned forever with zero signal. New `shared/selectors.ts`: `findStalledTasks`/`summarizeStalledTasks`/`describeStalledSummary` — any `queued` task idle past `STALL_THRESHOLD_MINUTES=2` with no assignee, bucketed by `no-agent-with-skill` / `all-agents-paused` / `idle` (the frontend can't see "provider not configured" directly, so `idle` just hints at checking the provider key, since that's the overwhelmingly common cause). New `ui/Banner.tsx` (+css) — persistent, non-dismissable alert, distinct from `Toast`. Wired into `MissionControlPage.tsx` and `TasksPage.tsx`. Verified live: a `QA`-skill task (only the parked `TesterAgent` covers it) triggered the correct banner on both pages after 2 minutes. `tsc`/lint clean. Committed directly to `main`, not a milestone branch.

**State (2026-07-14, MF-4): Employees profile + Organization live.** `Agent.paused` contract extension — the DTO already carried it (real since M0.7's auto-pause) but `adapters.ts`'s `adaptAgent` silently dropped it; threaded through `types.ts`/`adapters.ts`/`mocks/agents.json`/`mockStore.tsx`. New action `setAgentPaused` + generalized `usePatchAgentMutation` (now takes `paused` as well as `status`) so both providers can toggle the real `PATCH /agents/{id}.paused` field. `pages/employees/EmployeesPage.tsx` upgraded (skill chips, model, live workload count, Paused pill). New `pages/employees/EmployeeProfile.tsx` at `/employees/:id` — a full page (not a drawer), `ui/Tabs` with Overview/Tasks/Conversation/Memory/Performance/Settings; Tasks and Conversation tabs reuse MF-3's `nav.openTask` and `ui/ConversationThread` with zero new plumbing; Settings has Pause/Resume + Exit Focus Pod (the plan's "exit-focus lives here now"). New `pages/organization/OrganizationPage.tsx` — `Tabs` with Org Chart + Payroll. **Design call: the org chart is a purpose-built recursive tree (`OrgChartRow`), not `ui/Graph`** — `Graph` is a left-to-right layered DAG built for task workflows/edges; a strict manager-tree needing real `StatusDot`s and per-node expand/collapse didn't fit it without bolting on collapse state it has no concept of. Payroll tab is `BudgetPanel`'s JSX ported verbatim. `shared/nav.ts`'s `openAgent` now does real `navigate('/employees/${id}')` — the third and last of its three functions off legacy panels (only `openChat` remains); `OfficePage.tsx`'s avatar/roster click already routed through `nav.openAgent`, so it got real navigation for free. **Deleted:** `AgentProfilePanel.tsx`, `BudgetPanel.tsx`, `FocusPodPanel.tsx`; `PanelKey` shrank to 4 (`analytics`/`chat`/`announcements`/`flow`). Second `panels.css` cut: `.profile-*`/`.pod-*`/`.budget-*`/`.cost-row` removed (`.budget-*`/`.cost-row` ported into `OrganizationPage.css`; the rest had no surviving reader). **Done-when verified live in both modes** on real servers: mock mode — hire-with-manager updated the grid/org-chart/office-roster with no reload; pause/resume/exit-focus/cap-edit all worked with correct toasts; cross-page nav (profile→task drawer, task drawer→profile) worked with zero new code. API mode — this session's own `:5173` server (freed up since the prior session's dev server had exited) gave full read/write verification against the real backend: paused/resumed a real agent (`docker exec postgres psql` confirmed both DB states, demo left clean), edited a real payroll cap and confirmed it survived a hard refresh (real `PUT` round-trip). Console clean throughout. `tsc`/lint clean, zero hardcoded hex.

**State (2026-07-14, MF-3): Tasks kanban + drawer + review inbox live.** `ui/Kanban.tsx` (`KanbanBoard`/`KanbanColumn`/`KanbanCard`, generic drag-and-drop, first real workflow-board primitive) + `ui/ConversationThread.tsx` (message list + composer extracted from `ChatPanel`, now the shared single-channel chat widget — `ChatPanel` embeds it with `fill`, `TaskDrawer` embeds it fixed-height). `pages/tasks/TasksPage.tsx` rewritten from a placeholder into the real kanban board (`selectors.kanbanColumns`, queued/in_progress/pending_review/approved), with a "Needs rework" filter chip (`task.feedback != null`) that works identically whether a rejected task shows as `queued` (real backend, post-M0.6 two-hop reject→requeue) or `in_progress` (mock's simpler reject). **Drag restricted to legal transitions**: only Review→Done is honored (dispatches `approveTask`); every other drop is a no-op that toasts an explanation via `setNotice` — enforced in `TasksPage.handleDrop`, identically in both `VITE_USE_MOCKS` modes since it's pure client-side gating before any dispatch. New `pages/tasks/TaskDrawer.tsx` (real page, not a panel) — right-edge `Drawer` + `Tabs` (overview/subtasks/output/timeline/conversation), timeline built from `Task.events` (MF-2's contract extension, finally consumed) via new `EVENT_LABEL`/`eventTone` maps; approve/reject wired to the existing `approveTask`/`rejectTask` actions (no new contract). New `pages/tasks/ReviewInbox.tsx` at `/tasks/review` — same pending_review+flagged queue as the old `ApprovalsPanel`, so its count structurally matches `escalationCount`/the Tasks nav badge. Routing: `/tasks` (board), `/tasks/:id` (board + drawer, deep-links and survives refresh), `/tasks/review` (inbox) — react-router ranks the static `/tasks/review` above the dynamic `/tasks/:id` regardless of declaration order. `shared/nav.ts`'s `openTask` now does a real `navigate(`/tasks/${id}`)` instead of dispatching `openPanel workspace` — the second of its three functions to graduate off `openPanel` (only `openAgent`/`openChat` still route through legacy panels, pending MF-4/MF-6). **Deleted:** `dashboard/panels/WorkspacePanel.tsx` + `ApprovalsPanel.tsx` and their `PanelKey`/`LegacyPanelHost`/`NavSidebar` Classic-Panel entries; `AgentProfilePanel.tsx`/`TaskFlowPanel.tsx` (both still alive) had their `openPanel workspace` call sites swapped to `nav.openTask` instead of being left dangling. **First real `panels.css` cut** (711→ fewer lines): `.workspace-*`/`.task-row*`/`.approval-*` classes removed (ported into new `TaskDrawer.css`/`ReviewInbox.css` under non-colliding names — `.review-feedback` for the inbox, `.task-drawer-feedback` for the drawer, since both needed a differently-sized textarea); `.artifact-box`/`.section-title`/`.detail-actions`/`.status-badge` stayed since other still-alive panels use them. **Done-when verified live in both modes:** API mode confirmed via the shared `.env.local`'s real seeded company (read-only, through another session's already-running `:5173` dev server — this session's own throwaway server on `:5176` hit a CORS 403 since core-api's `atrium.cors.allowed-origins` defaults to `:5173` only, a known gap not fixed this session) — kanban rendered real tasks/agents correctly in all four columns. Mock mode fully driven on a throwaway `:5177`/`:5178` server: created→approved a Review-column task (badge 3→2, card moved to Done, toast fired); rejected a task with feedback from the drawer (task flipped to `in_progress` with `REVIEW FEEDBACK` box, "Needs rework (1)" chip appeared and correctly isolated it); illegal drag (In Progress→Done, simulated via real `DragEvent`s) left the card in place and showed the toast; `/tasks/:id` survived a hard refresh; Conversation tab sent a real message via `ConversationThread`; `ChatPanel` (now built on the same `ConversationThread`) still fully functional; `TaskFlowPanel`'s node click correctly navigated to `/tasks/:id` instead of the old panel dispatch. Console clean throughout. `npx tsc -b` and `npm run lint` clean (same 7 pre-existing fast-refresh warnings, zero new). `grep -rn "#[0-9a-fA-F]\{3,6\}"` zero matches across every new/touched file.

**State (2026-07-14, MF-2): primitives + real Mission Control live.** `shared/types.ts` gained `TaskEvent` + `Task.events: TaskEvent[]` — the one real contract extension, threaded through `adapters.ts`/`queries.ts` (API mode, zero extra requests — `useEnrichedTasks` already fetched `/tasks/{id}/events`) and `mockStore.tsx`/`mocks/tasks.json` (mock mode) together. New `shared/selectors.ts` — pure derivations (`computeKpis`, `companyBudgetBurn`, `companyPulse`, `liveMissionTask`, `synthesizeFeed`, plus `kanbanColumns`/`orgTree` for MF-3/MF-4) with no dependency on `storeTypes.ts`, safe to import from any page. `ui/` grew nine primitives: `PageShell`, `Card`, `StatCard`, `Tabs` (`?tab=`-synced), `StatusPill`, `EmptyState`, `Timeline`, `Feed`, `DataTable`, `Graph` (hand-rolled SVG layered tree, node status via new `--node-*`/`--glow-running` CSS vars). `pages/mission/MissionControlPage.tsx` rewritten from a teaser placeholder into the real home page: Company Pulse, KPI row, Live Mission hero, Workflow Preview (mini `Graph`), Output Preview, Agent Status grid, Budget Burn bar (80% warning), Live Activity feed. New `index.css`/`theme.ts` tokens are additive only (`--status-blocked`, `--node-*`, `--surface-{1,2,3}`) per the office's `--status-*`-by-name boot read.

**State (2026-07-14, MF-1): router + new shell live.** `react-router@7.18.1` added (library mode, no loaders). `App.tsx` → `BrowserRouter` → `shell/AppShell.tsx` (replaces `dashboard/DashboardLayout.tsx`, deleted along with `Sidebar/TopBar/RightRail/BotBar.tsx`). 11 real routes (`/`, `/tasks`, `/workflow`, `/employees`, `/organization`, `/projects`, `/knowledge`, `/reports`, `/office`, `/notifications`, `/settings`, `*`→`/`) — most are still thin `pages/PagePlaceholder.tsx` wrappers with an "Open classic X" button into the still-live legacy panel; `/office` and `/employees` are real. Every pre-MF-1 panel (Workspace/Chat/Analytics/Announcements/Approvals/Payroll/Task Flow/Profile/Focus) stays reachable via a temporary "Classic Panels" section in `shell/NavSidebar.tsx` + `shell/LegacyPanelHost.tsx` (the old `DashboardLayout` `PANELS` map, now a bridge deleted panel-by-panel as MF-2…MF-6 build real replacements). New `ui/` primitives: `Drawer.tsx` (right-edge slide-over, first real use: `ChatPanel.tsx` rehomed onto it, reachable from a persistent header icon) and `Toast.tsx` (replaces BotBar's notice banner). New `shared/nav.ts` (`useAppNav()`) is the one indirection between components and `openPanel` dispatches — later milestones swap its three functions to real navigation without touching call sites. `storeTypes.ts`/`AppState`/`Action` **untouched** — deliberate, keeps this milestone low-risk.

**Real bug found + fixed this session:** moving panels out of the office-scoped `.dashboard-office` container to a shell-level sibling made `PanelShell`'s `.panel-overlay` (`position:absolute;inset:0`) resolve against the *viewport* instead of a bounded region — silently swallowing clicks on the nav sidebar/header any time a legacy panel was open. Fixed by mounting `LegacyPanelHost` inside `.app-shell-content` (already `position:relative`) instead of at the shell root, and changing `Drawer.css`'s overlay from `position:fixed` to `position:absolute` to match. Verified fixed: can now switch between classic panels directly via the sidebar without closing the open one first.

**`OfficeCanvas.tsx` — two surgical fixes, zero behavior change otherwise:** (1) the `onAgentClicked → dispatch(openPanel profile)` effect moved out to new `pages/office/OfficePage.tsx` — canvas is now presentation-only; (2) the old `firstRoomRender` skip-first-render hack (silently dropped a non-default room on remount) replaced by reading the current room from a ref at `GAME_READY` time — `scenes/Game.ts` subscribes to `FOCUS_ROOM` before emitting `GAME_READY` in its own `create()`, so this always lands on a live listener, including right after a fresh mount on a non-Lobby room.

**Verified in browser, both `VITE_USE_MOCKS` modes:** all 11 routes deep-link correctly (survive hard refresh); `/office` renders the real roster, room chips update state and the camera pans (confirmed real, though visually slow in this specific automated-browser tab — `document.hidden===true` there, the same "Phaser's loop freezes in hidden tabs" caveat noted since session 3, not a regression); avatar click → real Agent Profile panel (confirmed via a direct `pointerup` emit on the sprite — coordinate-based clicking is unreliable for canvas hit-testing in this harness); Employees page card click → same panel; New Task / Hire Agent / Chat work from the header; every Classic Panel opens and correctly switches to another via the sidebar post-fix (Payroll showed real budget numbers matching session 6i exactly). `make check` clean (only the 7 pre-existing fast-refresh warnings, zero new ones).

**Files (`web/src/`):**
- `shell/` — `AppShell.tsx` (root layout — mounts `NavSidebar`/`GlobalHeader`/`AppRoutes`/`Toast`/`CommandPalette`, plus conditionally `ChatPanel`/`InviteAgentModal`/`NewTaskModal`), `NavSidebar.tsx` (primary IA only — no Classic Panels section since MF-6), `GlobalHeader.tsx` (title/clock/online-count/Cmd+K hint/Chat/Notifications/Settings/New Task/Hire Agent), `routes.tsx` (route table, 15 routes incl. `/tasks/:id`/`/tasks/review`/`/employees/:id`/`/projects/:id`) — **`LegacyPanelHost.tsx` deleted in MF-6**
- `pages/` — one dir per route, all real: `team/` (`TeamPage`, session 23 — replaced `office/`), `tasks/` (`TasksPage`/`TaskDrawer`/`ReviewInbox`), `employees/` (`EmployeesPage`/`EmployeeProfile`), `organization/`, `projects/` (`ProjectsPage`/`ProjectDetail`), `knowledge/`, `workflow/`, `reports/`, `notifications/` (`NotificationsPage`, MF-6), `settings/` (`SettingsPage`, MF-6) — `PagePlaceholder.tsx` still exists as a component but nothing routes through it anymore
- `ui/` — design-system-lite: `Drawer`, `Toast` (MF-1); `PageShell`, `Card`, `StatCard`, `Tabs`, `StatusPill`, `EmptyState`, `Timeline`, `Feed`, `DataTable`, `Graph` (MF-2); `Kanban`, `ConversationThread` (MF-3); `Banner` (post-MF-5 bug-fix session); `CommandPalette` (MF-6) — `Drawer`/`Tabs` both gained an a11y pass (focus trap, Escape, ARIA roles) in MF-6
- `shared/nav.ts` — the navigation-intent indirection layer; `openTask`/`openAgent` real routing since MF-3/4; `openChat` now dispatches `setChatOpen` (a real `AppState.ui` boolean) instead of the old panel-key pattern
- `shared/notifications.ts` — new MF-6: `buildNotifications` (pure) + `useNotifications()` hook, read-tracking on a `createDomainStore`-backed external store (shared across `GlobalHeader`'s bell and `NotificationsPage`)
- `shared/selectors.ts` — pure state derivations (MF-2) + shared `TASK_EVENT_LABEL`/`taskEventTone` (MF-5) + `agentWorkload`/`liveTeamZones` (session 23, the Team view's core)
- `shared/domains/` — net-new mock-only domains, outside `AppState`/`Action` (MF-5): `createDomainStore.ts` (tiny `useSyncExternalStore`-based store — MF-6 reused this same primitive for notification read-state, its second consumer), `projects.ts`, `knowledge.ts`, `workflows.ts`
- `dashboard/panels/` — down to `ChatPanel.tsx` (+ its own `ChatPanel.css`, MF-6), `InviteAgentModal.tsx`, `NewTaskModal.tsx` — `WorkspacePanel`/`ApprovalsPanel` (MF-3), `AgentProfilePanel`/`BudgetPanel`/`FocusPodPanel` (MF-4), `TaskFlowPanel`/`AnalyticsPanel` (MF-5), `AnnouncementsPanel`/`PanelShell` (MF-6) all retired; `panels.css` deleted, its content redistributed into `index.css`/`ConversationThread.css`/`ChatPanel.css` by actual ownership

**Pending:** none from the MF series — MF-1…MF-6 are all done. M-LN2 closes the last unbuilt learning-surface UI piece (review-queue). Chat/announcements/knowledge/projects/workflow-library stay mock-backed in both `VITE_USE_MOCKS` modes until their own backend milestones exist; Reports' trend/skills charts and Settings' model catalog stay similarly mock-vs-real split. `agent.status_changed` still isn't published by core-api — the Team view's task-derived zones are live via 4s polling, but its status-derived zones (in_focus/offline) only move on manual PATCHes. **CORS single-origin gap from MF-3 is still real, hit again this session (M-LN2):** `atrium.cors.allowed-origins` still defaults to `:5173` only — still worth a comma-separated default in `common/WebConfig.java` so parallel-session API-mode verification doesn't depend on port luck. `ui/PageShell` got its first real adoption (`TeamPage`, session 23) but every other page still hand-rolls its own header markup. **New from M-LN2:** `agentmind.LearningPipeline.ingest()` has never actually written a memory row in this project's history — it calls `embeddingClient.embed()` with no readiness guard, and no session has ever had `OPENAI_API_KEY` configured; a future session should either add the same `isReady()` skip `recall()` already has, or get a real key into the environment, before the reject→lesson→review→approve loop can be proven live end-to-end.

**Contracts:** UI spec `atrium-docs/01-product-spec.md §3–4` · API shapes `04-api-contract.md` · redesign plan `/Users/geetanshagrawal/.claude/plans/multi-agent-employee-platform-redesign-fluttering-crescent.md`.

Links: [[_Atrium]] · [[frontend-shared]] · [[web-office]] · [[core-api]] · [[milestones]]
