# office-realtime

**RETIRED 2026-07-15 (session 23), never built.** The planned Colyseus presence server (fork of SkyOffice's `server/`, milestones M2.4a–c) was cancelled with the office-view removal — see [[web-office]]. Nothing to delete: it was never vendored. The Redis outbox relay ([[realtimebridge]], live since M0.75) stays — it's generic infrastructure, currently without a subscriber; the dashboard's Team view gets its liveness from 4s HTTP polling instead.

Links: [[_Atrium]] · [[milestones]] · [[realtimebridge]]
