# execution

**What:** [[core-api]] module that does the actual AI work: `LlmClient` interface (AnthropicClient / OpenAiClient / GeminiClient, selected per agent row), `AgentRunner` (one loop per active agent, virtual threads), `PromptAssembler`.

**State: PROMPTS CARRY LEARNED CONTEXT (+ M-MEM1, 2026-07-15 session 15).** `PromptAssembler`'s system-prompt layout gained the "## What you have learned here" render branch (a short disclaimer line plus `- (kind) content` bullets from `bundle.memories()`), no longer omitted now that [[agentmind]]'s `SkillContextAssembler` actually populates that list some of the time; "## Reference material" (knowledge) stays omitted, still always empty until M-KN1. No other change to this module's own code this session — the real work (embedding calls, recall scoring, the claim-transaction design call) lives in [[agentmind]]; see that node for the full reasoning, including why memory recall's live embedding call runs inside the SAME claim transaction the skills lookups already used (a real extension of the "local-DB-only" rule this module's own `WorkBroker.claimNext` enricher hook was built around at M-CTX1). **Flagged, not fixed, this session:** `GoogleClient`'s `SimpleClientHttpRequestFactory` silently drops HTTP error response bodies in this Spring version (found while building agentmind's `OpenAiEmbeddingClient`, which uses `JdkClientHttpRequestFactory` instead) — `LlmException` messages from Gemini errors are generic rather than carrying the provider's real detail; the status-code-based `Kind` mapping itself is unaffected. Spawned as a follow-up task, out of this session's scope.

**Earlier — CONTEXT-AWARE AGENT LOOP LIVE (+ M-CTX1, 2026-07-14 session 14).** `LlmLoopRuntime.runOnce` step 1 now assembles the context bundle INSIDE `WorkBroker.claimNext`'s claim transaction (via a `Function<Task,ObjectNode>` enricher hook — see [[agentmind]] for the full boundary reasoning) so `task_events(claimed).payload.contextProvenance` lands same-tx with the claim's own audit event; the assembled `agentmind.ContextBundle` is captured for reuse and passed straight into `PromptAssembler.build(roleDef, task, feedback, bundle)` — no longer always null. `PromptAssembler`'s system-prompt layout gained a "## Your skills" section (14 §6); the "Reviewer feedback" heading also picked up an explicit attempt number. Full history below.

**Earlier — BUDGET-AWARE AGENT LOOP LIVE (M0.5a+M0.5b+M0.6+M0.7 done, 2026-07-13 session 6f).** `execution/spi/` verbatim 13 §1.1 (LlmMessage/ToolDef/ToolCall/Request/Result records, LlmProvider/LlmClient interfaces, LlmException w/ 7-kind taxonomy). `LlmRouter implements LlmClient`: provider bean map, catalog validation via registry's `ModelCatalogLookup`, Future-based timeout (`atrium.llm.timeout` 120s), in-attempt retries (RATE_LIMITED 3× / PROVIDER_DOWN 2×). `AnthropicClient` on official SDK `com.anthropic:anthropic-java:2.34.0` (maxRetries 0; missing key → AUTH; refusal → CONTENT_FILTERED; also implements new `ProviderReadiness.isReady()` — deliberately NOT on the frozen LlmProvider SPI — for the pre-dispatch gate). `LlmCostCalculator` (µUSD from catalog prices). **M0.5b: `LlmLoopRuntime implements AgentRuntime`** — replaces the M0.2 descriptor placeholder; one **virtual thread** per active agent (⚠️ forced a Java 17→21 bump, see below), `runOnce(companyId, agentId)` runs 13 §3.2 steps 0–7 and is public so tests drive it deterministically instead of racing the poll timer: 0 pre-dispatch gate (unready provider → park, log, skip tick, never claim), 1 `WorkBroker.claimNext` by skill set, maxAttemptsPerTask pre-check (flag `max_attempts` before any LLM spend), 2–3 `PromptAssembler.build(roleDef, task, feedback, bundle=null)` — **M0.6: `feedback` is no longer always null** — `taskService.latestRejectionFeedback(...)` is queried on every claim and passed straight through, so a reworked (rejected→requeued→reclaimed) task's prompt carries its reviewer's feedback; `bundle` stays null until M-CTX1. 4 `llmClient.complete` (empty tools list — no tool-loop yet, nothing can hallucinate tool_use), 5–6/7 `UsageRecorder.record` + `TaskService.complete` in **one explicit `TransactionTemplate` tx** (MANDATORY-propagation UsageRecorder joins it) — failure path flags via `FlagReasons` (13 §1.3 kind → reason string), never crashes the loop. **M0.7: `UsageRecorder.record` also calls [[accountability]]'s `BudgetLedger.recordSpend`** — but only when its own idempotent insert actually wrote a new row, so a deduped redelivery never double-counts spend on top of usage_records' own dedupe. `AgentLifecycleService` (registry) start/stop wired to hire/pause/unpause/budget-auto-pause. **Deliberately deferred:** tool-calling loop, periodic lease renewal during long calls, ContextAssembler (bundle always null until M-CTX1). **⚠️ Java bump 17→21:** 12 §8/13 §3.2 assume virtual threads (`Thread.ofVirtual()`, Java 21+) but 08-conventions.md pinned 17 — a real contract gap, resolved docs-first by bumping `java.version` (pom.xml, both Dockerfile stages, 08-conventions.md, CLAUDE.md) to 21 LTS.

**Key rules:**
- `PromptAssembler.build(roleDef, task, feedback?, bundle?)` is a **pure function that only sees those four inputs** — secrets structurally impossible in prompts. Rejection feedback included on retry (M0.6: [[routing]]'s `TaskService.latestRejectionFeedback` on every claim). Context bundle included since M-CTX1 ([[agentmind]]'s `ContextAssembler`, assembled inside the claim's own transaction so its provenance rides along same-tx with the `claimed` audit event).
- Every LLM call → `usage_records` insert with idempotency key `taskId:attempt` in the same tx ([[accountability]]) — redelivery never double-bills.
- Provider failure → `TaskService.flag()`, never crash the runner. Completion → artifact row + `pending_review`.
- API keys from env only, never logged.

**Contracts:** `atrium-docs/02-architecture.md §6` · [[data-model]] (usage_records, artifacts) · `05-module-specs.md §execution` · `08-conventions.md §Security` · `14-skills-memory-learning.md §6` (ContextAssembler/PromptAssembler layout).

Links: [[_Atrium]] · [[core-api]] · [[routing]] · [[accountability]] · [[agentmind]]
