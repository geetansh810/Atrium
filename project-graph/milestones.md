# milestones

**What:** the 26-milestone / 5-phase plan. Full cards + Done-when checks: `atrium-docs/07-milestones.md`; ready-made session prompts: `11-session-prompts.md`. One milestone per session.

**Phases:** 0 Foundation (M0.0–M0.8: OSS spike → scaffold → registry → routing → claim/lease → first agent → approval → budgets → task board) · 1 Validate (M1.1–1.3: content role, real pilot, fixes) · 2 Expand (M2.1–2.6: roles, dependencies, analytics, **SkyOffice fork M2.4a–c**, chat, panels) · 3 Multi-tenant (M3.1–3.5: auth, RLS, Stripe, onboarding, prod) · 4 Compliance (M4.1–4.3).

**Actual progress (2026-07-12) — note we deviated from doc order, frontend-first per user decision:**
- ✅ Dashboard shell + all panels ([[web-dashboard]] + [[frontend-shared]]) — the UI half of M0.8/M2.3/M2.5/M2.6, built early on mock data.
- ✅ [[web-office]] — SkyOffice client vendored (client half of M2.4a), stripped (M2.4b scope), agent avatars driven by the mock store (M2.4c preview). LimeZu license verified: paid tier required before commercial launch (06 §D).
- ✅ MB-0 (2026-07-12, session 5) — git repo initialized on `main`, root commit with web/ + docs + graph; root `.gitignore`/`.env.example`/`README`/`Makefile` (`make dev|check|test`, graceful no-ops for absent services).
- ✅ M0.1 (2026-07-12, session 5) — [[core-api]] scaffolded: Boot 3.5.6/Java 17, 8 module packages + boundary docs, common/ (TenantContext/problem+json/utils), Flyway V1+V2 applied on pgvector, compose stack boots health-UP, Testcontainers smoke green. Done-when verified in full.
- ✅ M0.2 (2026-07-12, session 5) — [[registry]] built: entities/repos/endpoints, RuntimeRegistry + llm_loop descriptor, AgentDirectory, V2_1 seed (3 templates + model catalog). 9/9 tests green; Done-when verified in full.
- ✅ M0.3 (2026-07-13, session 6) — [[routing]] writer + eventbus outbox: OutboxWriter (MANDATORY-propagation same-tx), TaskStateGuard, TaskEventRecorder, task create/list/get/events with keyset pagination, billing chain fields. 20/20 tests green; Done-when verified in full (isolation + exactly one task_events AND one outbox_events row per create).
- ✅ M0.4 (2026-07-13, session 6) — claim loop & lease: WorkBroker/PostgresWorkBroker (amended canonical claim SQL, id-keyed; 03 updated per 17), Worker API claim/renew (X-Agent-Id, 409 names holder), LeaseReclaimJob (advisory-lock singleton). 26/26 tests; Done-when verified (3×20 exactly-once, scheduled requeue w/ visible event, attempt++ per claim).
- ✅ M0.5a (2026-07-13, session 6) — LLM provider SPI + Anthropic: spi/ verbatim 13 §1.1, LlmRouter (catalog validation, timeout, taxonomy retries), AnthropicClient (official SDK), LlmCostCalculator. 48 tests; every 13 §1.3 row has a WireMock mapping test. Live real-key test written, ⚠️ pending a run with ANTHROPIC_API_KEY set (skipped otherwise by design).
- ✅ M0.5b (2026-07-13, session 6c) — agent runtime `llm_loop`, completes 07's M0.5: `LlmLoopRuntime` (real bean, virtual-thread poll loop, `runOnce()` public for deterministic tests), `PromptAssembler` (pure), `UsageRecorder` (ON CONFLICT DO NOTHING idempotency), `AgentLifecycleService` (hire/pause/unpause, afterCommit-deferred), `WorkBroker.claimNext` (skill-set claim), `TaskService.progress/complete/flag`. **Forced a Java 17→21 bump** (virtual threads) — docs-first fix in 08-conventions.md + pom.xml + Dockerfile. 50/50 tests (1 skipped); Done-when verified — real artifact from "reverse a string" (WireMock stands in for Anthropic), forced-requeue-mid-work redelivery test proves the idempotency key holds.
- ✅ M0.6 (2026-07-13, session 6e) — [[routing]] approval gate & audit: `TaskService.approve()` (blocked by open child tasks or open checklist subtasks, 03 invariant 5), `TaskService.reject({feedback})` (persists `rejected` status + feedback in the audit row, then requeues in the same tx so the redo gets a fresh attempt/idempotency key instead of colliding with the original usage_records row), `TaskService.latestRejectionFeedback()` wired into [[execution]]'s `LlmLoopRuntime.runOnce` so a reworked task's next prompt carries the feedback. Both endpoints outbox-publish via the existing `TaskEventRecorder`. 56/56 tests (1 skipped); Done-when verified — completed task sits in pending_review until acted on, `GET /tasks/{id}/events` shows the full chain, WireMock-captured retry request body contains the rejection feedback.
- ✅ M0.7 (2026-07-13, session 6f) — [[accountability]] budget enforcement: `BudgetService implements BudgetGuard, BudgetLedger` replaces `NoopBudgetGuard`; checks agent-scope AND company-wide caps per period, missing row = uncapped; agent breach auto-pauses via new `AgentDirectory.pauseForBudget` (registry), company-wide breach refuses without pausing an arbitrary agent; `UsageRecorder` increments `spent_tokens` only on a genuinely-new usage row (never double-counts a deduped redelivery); `budget.threshold` fires once per period via new `alerted_at` column (`V3` migration). New `BudgetBreachRecorder` (`REQUIRES_NEW` tx) fixes a real bug caught by tests: the auto-pause + `budget.exceeded` write can't live inside `canSpend`'s own transaction since the caller immediately throws to produce the 409, which would roll back and silently undo them. `@Lazy` on one constructor param in `JpaAgentDirectory` breaks a circular bean dependency the new `pauseForBudget` call chain introduced. 60/60 tests (1 skipped); Done-when verified — low cap blocks claim + agent shows paused in roster + resumes after raise-cap/unpause, company-wide cap blocks without pausing anyone, threshold event fires exactly once across two spend calls.
- ✅ M0.75 (2026-07-13, session 6g) — [[realtimebridge]] outbox relay (eventbus half of [[agent-platform]]): `OutboxRelay` (`@Scheduled` 250ms, advisory-lock singleton, claim+publish+mark in one tx via new `OutboxRelayGateway`) publishes to Redis `atrium:events:{companyId}` per [[realtime-events]]; `EventCursorWorker` durable-consumer base (no concrete consumer yet — M-LN1/M2.3 are first); `OutboxRetentionJob` (nightly 14-day/cursor-floor purge). 66/66 tests (1 skipped); Done-when verified — real Redis subscriber receives `task.created` within 1s of task creation, and a simulated mid-batch crash (forced publish failure) leaves rows unpublished until the fault clears, then a later tick republishes them (never lost).
- ✅ M0.8 (2026-07-13, session 6h) — [[web-dashboard]] on the real API, the mock→API swap doc 17 redefined this milestone as: new `shared/api.ts`/`adapters.ts`/`queries.ts` (React Query) + a provider split (`mockStore.tsx` vs `apiStore.tsx`, picked by `VITE_USE_MOCKS` at module load) behind the unchanged `store.tsx` `useApp()` contract — only one component (`InviteAgentModal`) needed an edit. Chat/announcements/analytics stay mock-backed in both modes (no backend endpoint exists for any of them yet). New `web/scripts/seed-dev.mjs` creates a demo company + 3 real (Anthropic-only) agents. **Found and fixed two real core-api bugs by actually running the browser against it:** no CORS config at all (new `common/WebConfig.java`), and `TenantContextFilter` rejecting CORS preflight `OPTIONS` before Spring's CORS handling ever ran. 66/66 tests still green after both fixes. Done-when verified for create→list→detail→events→budget against the real API in-browser; the claim→work→approve→budget-moves leg needs `ANTHROPIC_API_KEY` (not present in the session env) — deferred, documented as the first thing to close next session if a key is available.
- ☐ Everything else. Real M2.4a–c server halves (Colyseus vendor, room tokens, Redis presence) still pending.

**Rev C (2026-07-12):** backend sequence now lives in `atrium-docs/17-backend-execution-plan.md` ([[agent-platform]]) — amends M0.1/M0.2/M0.4, splits M0.5a/b, redefines M0.8 (mock→API swap), adds MB-0, M0.75 and the M-SK/CTX/MEM/LN/KN/AR agent-depth series before/around Phase 1. M0.0's Paperclip half ✅ (findings in `atrium-docs/notes/`).

**Rev D — Frontend redesign (2026-07-14, deviated from doc order again, user-initiated): ✅ complete as of MF-6 (session 12).** A second parallel series, "MF-1…MF-6", supersedes the office-canvas-first dashboard with a Linear/Notion/Datadog-style AI Operating System. Not in `atrium-docs/` — the full plan (context, architecture, milestone cards, risks) lives at `/Users/geetanshagrawal/.claude/plans/multi-agent-employee-platform-redesign-fluttering-crescent.md`. Ran interleaved with the M-SK/CTX/MEM series above (no file overlap: MF touched `web/src/{shell,pages,ui,shared/nav.ts}`; backend milestones touch `core-api/`). SkyOffice is kept fully functional throughout, demoted to one optional `/office` route.
- ✅ MF-1 (2026-07-14, session 7) — [[web-dashboard]]: `react-router` added, new `shell/`+`pages/`+`ui/` structure, old `dashboard/{DashboardLayout,Sidebar,TopBar,RightRail,BotBar}.tsx` deleted, every pre-existing panel kept reachable via a temporary "Classic Panels" bridge. Office demoted to `/office`, two surgical `OfficeCanvas.tsx` fixes (agent-click extraction, room-remount fix), zero behavior change otherwise. Found+fixed a real click-through bug (overlay scoping) via browser testing. Verified live against the real API + in mock mode; `make check` clean.
- ✅ MF-2 (2026-07-14, session 8) — [[web-dashboard]]: nine new `ui/` primitives (`PageShell`/`Card`/`StatCard`/`Tabs`/`StatusPill`/`EmptyState`/`Timeline`/`Feed`/`DataTable`/`Graph`), new `shared/selectors.ts`, and the `Task.events` contract extension (landed in both providers + mock fixtures). Real `MissionControlPage` (Pulse/KPIs/Live Mission/Workflow Preview/Output Preview/Agent Status/Budget Burn/Live Activity) replaces the teaser placeholder. Verified live: created+claimed+completed+approved a real task via the API-mode UI with KPIs/feed/budget updating within one poll each step; mock mode verified on a separate throwaway dev server (didn't disturb the other session's :5173 instance). `tsc`/lint clean, zero hardcoded hex (grep-verified).
- ✅ MF-3 (2026-07-14, session 9) — [[web-dashboard]]: new `ui/Kanban.tsx` (generic drag-and-drop board) + `ui/ConversationThread.tsx` (extracted from `ChatPanel`, now shared by it and `TaskDrawer`). Real `pages/tasks/{TasksPage,TaskDrawer,ReviewInbox}.tsx` replace `WorkspacePanel`/`ApprovalsPanel` (both deleted). Kanban drag restricted to Review→Done (=approve); every other drop toast-explains agents drive status. TaskDrawer's Timeline tab is the first real consumer of MF-2's `Task.events` extension. `shared/nav.ts`'s `openTask` now does real routing instead of `openPanel`. First real `panels.css` cut (workspace/approval classes removed, ported into new page-scoped CSS). Verified live in both modes: API mode confirmed read-only against the shared seeded company; mock mode fully driven (approve/reject/drag/refresh-survival/conversation-send) on throwaway dev servers. `tsc`/lint clean, zero hardcoded hex.
- ✅ MF-4 (2026-07-14, session 10) — [[web-dashboard]]: `Agent.paused` contract extension (DTO already had it, adapter was dropping it) + new `setAgentPaused` action (generalizes `usePatchAgentMutation` to accept `paused`, not just `status`). Real `pages/employees/{EmployeesPage,EmployeeProfile}.tsx` (upgraded grid + full profile page, 6 tabs incl. Settings pause/resume + exit-focus) and `pages/organization/OrganizationPage.tsx` (Org Chart + Payroll tabs) replace `AgentProfilePanel`/`BudgetPanel`/`FocusPodPanel` (all three deleted). Org chart is a purpose-built recursive tree (not `ui/Graph` — a strict manager-tree with expand/collapse and real `StatusDot`s didn't fit the DAG-shaped primitive cleanly). `nav.ts`'s `openAgent` now does real routing — all three `nav.ts` functions except `openChat` are off legacy panels now. Second `panels.css` cut (profile/pod/budget classes removed or ported). Verified live in both modes on real servers: mock mode — hire-with-manager updated grid+chart+office roster with no reload; pause/resume/exit-focus/cap-edit all worked with correct toasts. API mode — this session's own `:5173` server (freed up since the prior session's had exited) gave full read/write verification: paused/resumed a real agent (`docker exec postgres psql` confirmed both DB states), edited a real payroll cap and confirmed it survived a hard refresh. `tsc`/lint clean, zero hardcoded hex.
- ✅ MF-5 (2026-07-14, session 11) — [[web-dashboard]]: three net-new mock-only domains (`shared/domains/{projects,knowledge,workflows}.ts` + fixtures), none touching `AppState`/`Action`. `projects.ts` uses a small hand-rolled external store (`createDomainStore.ts`, `useSyncExternalStore`) rather than per-component `useState`, since `ProjectsPage`↔`ProjectDetail` need to share create/link/unlink state across navigation. Real `pages/projects/{ProjectsPage,ProjectDetail}.tsx` (overview/tasks/artifacts/agents/timeline/budget tabs; tasks tab reuses `ui/Kanban` filtered to a project's linked real task ids, with an add/remove control for the client-side-only mapping), `pages/knowledge/KnowledgePage.tsx` (search + `ui/Drawer` doc detail), `pages/workflow/WorkflowPage.tsx` (port+upgrade of `TaskFlowPanel` onto `ui/Graph` — its first non-mini, non-org-chart real DAG consumer — plus a separate mock "Workflow Library" section), `pages/reports/ReportsPage.tsx` (port of `AnalyticsPanel` + three new real-derived sections: status distribution, per-agent budget burn, completion count, alongside the pre-existing always-mock trend/skills charts). Mock-only sections get a "Sample data" `StatusPill` in API mode only (`!USE_MOCKS`), never in mock mode. Deleted `TaskFlowPanel.tsx`/`AnalyticsPanel.tsx`; `PanelKey` shrank to 2 (`chat`/`announcements` — only `openChat` still routes through a legacy panel, pending MF-6). Third `panels.css` cut: `.status-badge*`/`.flow-*` removed outright (no successor); `.kpi-grid`/`.bar-*`/`.perf-*` ported into `ReportsPage.css`; the legacy `.stat-card*` block also removed — it turned out to duplicate `ui/StatCard.css`'s class names with different rules (AnalyticsPanel was its only other reader, a real MF-2-era CSS collision closed as a side effect). **Found and fixed a second real pre-existing bug in passing:** `OrganizationPage.tsx`'s Payroll tab (`.budget-row`/`.budget-cap-btn`/`.cost-row`/`.mono` etc.) had zero matching CSS anywhere in the codebase despite MF-4's session notes claiming it was ported — added the missing rules to `OrganizationPage.css`. Verified live in both modes: API mode (this session's own `:5173` server) — linked a real seeded task into a mock project, saw it render in the right Kanban column with its real artifact/timeline/budget-cost data, "Sample data" pills correctly present; mock mode (throwaway `:5180` server) — pre-seeded project→task links rendered a populated board/timeline/budget across all tabs, Workflow page's graph showed correct live node colors (running/completed) for a real multi-step mock task chain, no "Sample data" pills anywhere. Console clean both modes. `tsc`/lint clean (same 7 pre-existing warnings, zero new). `grep -rn "#[0-9a-fA-F]\{3,6\}"` zero matches across every new/touched file.
- ✅ MF-6 (2026-07-14, session 12) — [[web-dashboard]]: **final milestone — the MF series is complete.** Real `pages/notifications/NotificationsPage.tsx` (derived feed via new `shared/notifications.ts`, merging task events + announcements; composer ported from deleted `AnnouncementsPanel`) and `pages/settings/SettingsPage.tsx` (company info, live model catalog, mode indicator — each card a `Mock*`/`Api*` pair picked once at module scope, avoiding a rules-of-hooks violation). New `ui/CommandPalette.tsx` (Cmd+K: pages/agents/tasks substring search + New Task/Hire Agent/Pause All, the last mirroring M0.8's `toggleSubtask` mock-real/API-noop precedent). **Real bug caught live:** `GlobalHeader`'s bell and `NotificationsPage` each held an independent `useState` copy of the read-notification-id set, so the badge never cleared after visiting the page — fixed by moving that state onto MF-5's `createDomainStore` external store (its second consumer) so every `useNotifications()` caller shares one source of truth. **Contract cleanup went further than "shrink `PanelKey`" — removed it entirely**: `openPanel`/`closePanel`/`selectedAgentId`/`selectedTaskId`/`railVisible`/`toggleRail` all deleted, replaced by a single `chatOpen: boolean` + `setChatOpen` since Chat is now a permanent AppShell-level drawer (like the modals) rather than routed through the panel-key pattern. Deleted `AnnouncementsPanel.tsx`, `PanelShell.tsx`, `LegacyPanelHost.tsx`, and `NavSidebar`'s "Classic Panels" section entirely. `panels.css` (355 lines) deleted outright — content redistributed by actual ownership, not just moved wholesale: global atoms into `index.css`; `.chat-msg*`/`.chat-input*` (discovered to be owned by `ui/ConversationThread.tsx`, not `ChatPanel`) into `ConversationThread.css`; real `ChatPanel`-only classes into new `ChatPanel.css`. a11y pass: `ui/Drawer.tsx` gained a real focus trap + Escape + focus-restore; `ui/Tabs.tsx` gained `role="tabpanel"` + arrow-key navigation. Verified live in both modes (API mode against the real seeded company; mock throwaway `:5181`): Cmd+K found a real agent and task by substring and navigated to each; bell badge synced correctly post-fix; Settings showed the live 4-model catalog in API mode; Pause All genuinely paused all 6 mock agents. `grep -rn "openPanel"` and hardcoded-hex greps both zero matches. `tsc`/lint clean (7 pre-existing + 2 new same-category warnings). `make check` exit 0.

- ✅ M-SK1 (2026-07-14, session 13) — [[agentmind]] module goes live: skills registry (14 §1). Schema was already applied at M0.1 (`V2__agent_platform.sql` pasted the full Rev C schema verbatim, ahead of the milestones that build on each table) — this milestone is Java/API only. `Skill`/`RoleDefinitionSkill`/`AgentSkill` entities (first `@IdClass` composite-key use in the codebase), `SkillService` (CRUD/versioning/attach/detach/mind-view), new `V5__seed_platform_skills.sql` (2 platform skills per coder/tester/research template). Hire-time auto-attach solved with a registry-owned hook interface (`RoleSkillAttachment`) implemented by agentmind, keeping the `agentmind → registry` dependency direction one-way. Design call: role-skill attach restricted to company-owned role definitions (attaching to a shared global template would leak across tenants). 82/82 tests green (2 skipped); Done-when verified — hire auto-attaches template skills, attach/detach reflected in `/mind`, two-company isolation + kind filter + versioning dedup all correct.
- ✅ M-CTX1 (2026-07-14, session 14) — [[agentmind]]'s `ContextAssembler` v1: skills into prompts (14 §6), wired into [[execution]]'s `LlmLoopRuntime` step 2. Skills-only (memories/knowledge slots — `MemoryHit`/`KnowledgeHit` placeholder records — stay empty until M-MEM1/M-KN1). Deterministic assembly under a token budget (`runtime_config.contextBudgetTokens` default 4000, hard-capped at 30% of the model's catalog context window, chars/4 heuristic): role-attached skills in role position order then the agent's own by proficiency desc, item-granular truncation (whole body if it fits, else name+description fallback, else drop and keep trying subsequent items), only `platform`/`company` trust-level skills ever included. **Real cross-module design call:** `task_events(claimed).payload.contextProvenance` had to land in the SAME transaction as the claim + its audit event (03 invariant 1), but [[routing]]'s `WorkBroker` must not depend on agentmind/execution — solved with a plain-JDK `Function<Task,ObjectNode>` enricher hook on `claimNext`, invoked inside its existing transaction; `LlmLoopRuntime` passes a lambda that assembles the bundle once and reuses it for the prompt (no second assemble() call). `PromptAssembler` gained a "## Your skills" system-prompt section; `ContextBundle`/`SkillExcerpt` live in `agentmind` (not `execution`) since agentmind must never import execution types. 90/90 tests green (2 skipped); Done-when verified — 7 unit-style tests (fits-all/fallback/truncation/ordering/trust-filter/budget/determinism) plus an integration test showing the claimed event's real `contextProvenance` array and a WireMock-captured prompt containing "## Your skills".

- ✅ M-MEM1 (2026-07-15, session 15) — [[agentmind]]'s memory store + recall live: `EmbeddingClient` SPI + `OpenAiEmbeddingClient` (REST, `text-embedding-3-small`/1536-dim), `PgVectorMemoryStore implements MemoryStore` (raw-JDBC pgvector cosine search — no Hibernate vector-type mapping, only this class ever touches the `embedding` column), 14 §2 scoring formula verbatim (0.75·similarity + 0.15·recency_decay + 0.10·use-count-bonus, threshold 0.30), scope-set = agent ∪ role ∪ company (task-scope excluded from cross-task recall). `SkillContextAssembler` (unrenamed) gained the memories step: `recall(k=12)`, preference/lesson ordered before fact/summary, item-granular truncation at ≤35% of budget. **Real design call:** memory recall's live embedding call runs INSIDE the claim's own transaction (extending M-CTX1's "local-DB-only" enricher-hook rule, since queryText is task-specific and can only be embedded post-claim) — accepted, documented, degrades to empty (never throws) when embeddings aren't configured so the existing demo/claim loop can't be silently broken by a missing key. New `MemoryService`/`MemoryController` (browse/seed/forget, 16 §3 — review-queue deferred to M-LN1) + `MemoryTtlArchiver` (nightly, advisory-lock singleton). **Real bug found+fixed:** `SimpleClientHttpRequestFactory` silently drops HTTP error response bodies in this Spring version — `OpenAiEmbeddingClient` uses `JdkClientHttpRequestFactory` instead (pinned HTTP/1.1 for WireMock compatibility); the same latent bug in [[execution]]'s `GoogleClient` was flagged as a follow-up task, not fixed inline. 110/110 tests green (3 skipped); Done-when verified — seeded company preference reaches the next task's real LLM request under "## What you have learned here", scope/status isolation (agent/company/pending-review/archived all correctly excluded), recall stays under 100ms at 10k memories (dedicated perf test, HNSW-indexed fixture generated via a single in-DB SQL statement rather than a slow JDBC batch).
- ✅ M-LN1 (2026-07-15, session 16) — [[agentmind]]'s `LearningPipeline` (14 §5) goes live, the first real `eventbus.EventCursorWorker` subclass: consumes `task.rejected|approved|completed`, extracts via a cheap-tier model from the SAME provider an agent already uses, governs each item deterministically (`decideStatus`: only agent-scope lesson/summary auto-activate, everything else — every role/company write, every fact regardless of scope — lands `pending_review`), dedupes on ≥0.92 cosine similarity (bump `importance` instead of a duplicate insert), and metered via a new shared `accountability.UsageLedger` (`learn:{eventId}` idempotency). New review-queue + review-action endpoints on `MemoryService`/`MemoryController` (approve/reject, optional `promoteScope`/`asSkill`-draft-skill). **Two real cross-module design calls:** (1) `EventCursorWorker`'s constructor-param repository types had to go from package-private to public — M0.75 built the base class but its own test never subclassed it from outside `eventbus`, so this was the first time the visibility gap actually mattered; (2) `LearningPipeline` needs an LLM call + usage metering but must never import `app.atrium.execution` (12 §2) — solved by depending on the neutral `execution.spi.LlmClient` only (never `execution.LlmCostCalculator`/`ProviderReadiness`/`UsageRecorder`) and by extracting a new `accountability.UsageLedger` interface (moved out of `execution.UsageRecorder`, which now just supplies the `taskId:attempt` key format and delegates) so both callers share one insert-once-then-spend implementation; boundary rule `agentmind → accountability` added to 12 §2 alongside it. **A real self-invocation bug caught by review before it shipped:** `LearningPipeline`'s own `@Scheduled poll()` calling the inherited `@Transactional pollOnce()` via unqualified `this.pollOnce(...)` would have bypassed Spring's proxy entirely (classic self-invocation trap) — every `MANDATORY`-propagation write inside `handle()` (`OutboxWriter`, `UsageLedger`) would have thrown `IllegalTransactionStateException` at runtime despite compiling and even despite `EventCursorWorkerTest`'s own direct-call tests passing (that test never exercises a `@Scheduled`-style self-call). Fixed with a `@Lazy` self-injected proxy reference, same idiom `JpaAgentDirectory` already uses for its own circular-dependency case. **A real Hibernate staleness bug caught by the tests, not by inspection:** `MemoryService.review()`'s final read used JPA `findById` after several raw-JDBC writes (`PgVectorMemoryStore`'s whole-class convention) to the SAME row already sitting in the persistence context's first-level cache — silently returned the pre-mutation snapshot (`status:"pending_review"` on a call that just approved it) in 3 different tests; fixed with `entityManager.refresh(memory)` instead of a second `findById`. 115/115 tests green (3 skipped, unchanged live-key tests). `make check` clean. Done-when verified — reject a task with feedback "never use passive voice; our brand name is X-Corp" → agent gets an auto-active lesson AND a pending company fact (new `LearningPipelineTest`, WireMock-backed); approving the fact makes it recall for a different agent (company scope is unconditionally in every recall's scope-set); replaying the same event via a rewound consumer cursor creates no new memory rows and no new `usage_records` row (dedup bumps importance instead); a rejected memory never appears in recall (new `MemoryApiTest` cases, no LLM needed since `pending_review`/`rejected` were already excluded by construction — the test guards that reject-via-review doesn't regress that).

- ✅ M-KN1 (2026-07-15, session 17) — [[agentmind]]'s knowledge ingestion goes live (14 §3), the last piece of the depth series. New `KnowledgeService`: ingest (chunk via new `KnowledgeChunker` — ~800-token/100-overlap sliding window on the chars/4 heuristic — then batch-embed via `EmbeddingClient.embed(List)`, its first real caller, then a raw-JDBC batch insert of `knowledge_chunks` since the vector column has no Hibernate mapping), list/archive, `attachToRole`, and `recall` (top-3 chunks, plain cosine similarity ≥0.35). No separate `KnowledgeStore` SPI the way `MemoryStore` exists — 14 §3 doesn't describe knowledge as a swappable contract, so `KnowledgeService` does both validation and the raw-JDBC work directly, called straight from `SkillContextAssembler`. Knowledge scoping is structurally different from memories: no `scope` column — a doc surfaces only for the specific `role_definition_id` rows it's attached to via `role_definition_knowledge`, mirroring `SkillService.attachToRole`'s "company-owned role definitions only" restriction (and its consequence: an agent hired from a global template can never get company-specific knowledge on its role — only a custom company-owned role can). `ContextBundle`'s third slot is populated; `PromptAssembler`'s "## Reference material" section (left un-added since M-CTX1) finally renders. **Real bug caught by the first test run — mirror image of M-LN1's Hibernate-staleness bug:** the raw-JDBC chunk insert (FK to `knowledge_docs.id`) ran before Hibernate had flushed the JPA-saved doc row to the connection, failing every ingest with a FK violation until an explicit `entityManager.flush()` was added between the two writes. 127/127 tests green (3 skipped); Done-when verified — a custom "content-writer" role's agent recalled an attached "Brand guide" doc into its prompt under "## Reference material"; a "coder"-template agent in the same company did not (role attach respected, not global).

- ✅ M1.1 (2026-07-15, session 18) — Business role via registry only, Phase 1's first milestone and the extensibility proof (`atrium-docs/07-milestones.md` line ~51, `11-session-prompts.md` card — no doc-17 Paste list, no code involved at all). Built entirely as **data**, via `curl` against the already-running dev stack: a real company "Agrawal Namkeen" (not another `seed-dev.mjs` fixture — the actual pilot business), a company-owned `role_definition` (`key:"content"`, [[registry]]'s existing `POST /role-definitions` path — the same "custom role, not global template" flow M-SK1/M-KN1 already established for skills/knowledge attach), one agent ("Priya", `google`/`gemini-3.1-flash-lite` since only `GOOGLE_API_KEY` is live this session). Done-when verified literally: a real Diwali-gift-box Instagram-caption task claimed and completed by Priya through the fully unmodified [[routing]]/[[execution]]/[[accountability]] pipeline (`created→claimed→progress→completed→approved` audit chain, real `usage_records` row with real token counts), and `git diff --stat core-api/` confirmed empty both before and after — "roles are data" held for a brand-new tenant and role with zero source changes. M1.2 (real pilot run, a non-Claude reviewer) is next and needs no new code — the company/role/agent created here are left live in the dev DB for it to reuse directly.

- ✅ M1.2 (2026-07-15, session 19) — Real pilot run, verified. A **pure process milestone** (`atrium-docs/07-milestones.md` line 55) — the only prep needed was pointing the dashboard at the right tenant: `web/.env.local`'s `VITE_COMPANY_ID` still pointed at the old "Atrium Demo Co" fixture from `seed-dev.mjs`, not M1.1's real "Agrawal Namkeen" company, so it was repointed and the dev server (already-running `core-api`/Postgres/Redis) restarted against it — confirmed live in-browser (Priya online, M1.1's task/audit-trail rendering correctly on Mission Control). The user then created and reviewed a real task themselves via the dashboard UI, unassisted — a second "Research about competition" task (`requiredSkill:"content"`), claimed by Priya within 2s, completed with genuine on-brand competitor-research copy (naming real local sweet shops, Agrawal Namkeen's own voice), then approved by the user. Verified post-hoc via JDBC: full audit chain `created→claimed→progress→completed→approved`, real `usage_records` row (188 in / 194 out tokens, 97 µUSD, idempotency key `<taskId>:1`). Literal Done-when met — "a real task approved/rejected by someone else, unassisted" — though the "non-Claude reviewer" in the doc's phrasing turned out to be the user themselves rather than an external Agrawal Namkeen employee; no friction worth writing up, so **M1.3 (feedback fixes) is not triggered** — nothing broke, nothing was confusing enough to log.

- ✅ M2.1 (2026-07-15, session 20) — PM & Designer roles, Phase 2's first milestone, built "exactly like M1.1" (`atrium-docs/07-milestones.md` line 65, `11-session-prompts.md` card — no doc-17 Paste list, no code at all). Pure data again, via `curl` against the already-running dev stack, reusing the live "Agrawal Namkeen" tenant from M1.1/M1.2 rather than a fresh company: two company-owned `role_definition`s (`key:"product"`/"Product Manager" and `key:"design"`/"Designer", both via [[registry]]'s existing `POST /role-definitions`), each with a business-grounded system prompt/output contract (product sizes gift-box pricing tiers for the real product line; design writes printer-executable packaging specs, since the LLM can't generate images) — not generic PM/Designer boilerplate. Two agents hired (Rohan/product, Meera/design, both `google`/`gemini-3.1-flash-lite`, the only live key). Done-when verified literally: one task each through the fully unmodified pipeline — Rohan sized a real 3-tier Diwali gift-box pricing structure (Rs 249/499/899) with genuine risk callouts, Meera wrote a full packaging spec (colors/typography/motifs/production notes) for a Rs 999 tier — both claimed within ~2s, completed in ~3s, full audit chain `created→claimed→progress→completed→approved` (JDBC-verified), real `usage_records` rows (441/400 tokens ≈204µUSD and 462/418 tokens ≈213µUSD), both approved by Claude to close the loop (no separate human-reviewer requirement on this card, unlike M1.2). `git diff --stat core-api/` empty before and after — "roles are data" holds again, this time for two SIMULTANEOUS roles on one tenant, not just one.

- ✅ M2.2 (2026-07-15, session 21) — Task dependencies & Task Flow API, the first M2.x card needing real code. New [[routing]] `TaskService.decompose`/`completeWithDecomposition`: exact-once child fan-out fingerprinted on `task_decompositions(parent_task_id, plan_artifact_id)` where `plan_artifact_id` is the parent's OWN completion artifact (no separate "plan" artifact kind) — a raw `INSERT ... ON CONFLICT DO NOTHING` decides at the DB level whether a call gets to create children or hands back the existing `child_task_ids` as a no-op. New `GET /tasks/{id}/flow` (any task in the chain → walks to the root, returns the whole descendant graph via a `WITH RECURSIVE` CTE). New [[execution]] tool `create_child_tasks` (`ChildTaskTool`), offered to the model only when the claimed task's role has it in `allowed_tools` (a column that had existed unused since M0.2) — single-shot, not a multi-turn loop (Anthropic's tool_use/tool_result protocol needs content blocks `LlmMessage`'s plain-string content can't carry yet). Approve-blocked-on-open-children turned out to ALREADY exist since M0.6 (this milestone's own card text, and this doc's prior "Likely next" note, both mis-stated it as partial/subtasks-only — corrected after reading the actual `TaskService.approve` code). 3 new `TaskDecompositionTest` cases (spawns children/blocks-until-both-ship, exact-once replay, flow graph from both a parent and child anchor) + 1 new `LlmLoopRuntimeTest` case (real WireMock-stubbed Anthropic tool_use response drives the whole path through `runOnce`). 131/131 tests green (3 skipped). **Verified live against the real Agrawal Namkeen tenant, not just the test suite:** a new "product" role-definition version with `allowedTools:["create_child_tasks"]`, a new PM agent hired against it, and a real Gemini call that genuinely chose to invoke the tool — spawned 2 real child tasks (design+content) with a correct `task_decompositions` row, `/flow` returned the live 3-node graph, and `/approve` 409'd while the children were still open.

- ✅ M2.3 (2026-07-15, session 22) — Budget ledger UI + analytics rollups, the first M2.x card since M2.2 to need real code. New `agent_stats_daily` table (new `V6__agent_stats_daily.sql`, split out of the old planned "V3 communication_office" bundle since channels/messages/announcements/office_layout aren't needed until M2.4c/M2.5) — schema deviates from 03's original sketch by adding a `skill` column to the primary key (`company_id, agent_id, skill, day`) plus `cost_micro_usd`, a docs-first amendment so "Top Skills Used" is a plain GROUP BY with zero cross-module reads. New [[accountability]] `StatsRollupWorker extends EventCursorWorker` — the SECOND real durable-consumer subclass after M-LN1's `LearningPipeline` — consumes `task.completed|approved|rejected` straight from outbox payload fields only (`agentId`/`requiredSkill`/`attempt`, three small additive fields `TaskService` now puts on those events), never touching a `Task` entity or `TaskRepository` — accountability has no dependency on routing, and this consumer proves that boundary holds even for a feature that conceptually "belongs" to task lifecycle data. New nightly `StatsRollupReconciliationJob` (advisory-lock singleton, same idiom as `OutboxRetentionJob`/`MemoryTtlArchiver`) — a genuine drift-correcting full recompute of the trailing 2 UTC days straight from `outbox_events` (still retained — no durable consumer's cursor is ever purged ahead of), not just a redundant re-run of the incremental path. Five analytics endpoints (`summary`/`tasks-7d`/`agent-performance`/`top-skills` per 04, plus a new **`cost-per-task`** — not in the original 17 sketch, added docs-first to 04 §Accountability since the Budget Ledger's per-task cost table needs real per-task granularity `agent_stats_daily`'s per-day/skill grain structurally can't give; it queries `usage_records` directly instead). New `AnalyticsRepository`/`AnalyticsService`/`AnalyticsController` in [[accountability]], all raw-JDBC aggregate reads (no Hibernate mapping on either source table, matching `PgVectorMemoryStore`'s established idiom for aggregate-heavy tables). Web: [[web-dashboard]]'s `ReportsPage` gained a real Agent Performance leaderboard section and now wires real `tasks-7d`/`top-skills`/`agent-performance`/`summary` data in API mode (mock fixtures + "Sample data" pill unchanged in mock mode) via a `useMockAnalytics`/`useRealAnalytics` hook-pair split (same "whole hook-set picked once at module scope" pattern `SettingsPage` established at MF-6, avoiding a rules-of-hooks violation); an honest "Focus Time isn't tracked yet" caption replaces a fabricated number, since no presence source exists until M2.4c. `OrganizationPage`'s Payroll tab gained a real period selector (`<input type="month">`) + real cost-per-task rows in API mode (mock mode keeps the static fixture with a "doesn't vary by period" note) via the same Mock/Api component-pair split. New `shared/format.ts` `formatCost()` — real per-task costs at this demo's usage scale are sub-cent, and plain `toFixed(2)` was silently rounding every one of them to "$0.00". 10 new backend tests (`StatsRollupTest`, `AnalyticsApiTest`, `StatsRollupReconciliationJobTest`) — 141/141 core-api tests green (3 skipped, unchanged live-key tests), zero regressions from the additive event-payload fields. **Done-when verified live, not just by test suite:** rebuilt the core-api container against the real Agrawal Namkeen tenant, created a real "write a Diwali sale tagline" task, kicked Priya's loop alive (agent loops don't auto-restart after a container rebuild — a gap already documented from a prior session), watched it claim→complete with genuine Gemini-generated copy→get approved, and confirmed all 5 analytics endpoints reflected it within one 5s poll tick (`tasksCompletedToday`, `agent-performance` leaderboard, `top-skills` share, `tasks-7d` bar) — verified again in-browser on the real dashboard (Reports page's KPIs/leaderboard/skills, Organization page's period-scoped cost-per-task table with the new precise cost formatting, period selector genuinely re-querying and showing an honest empty state for a month with no spend). `tsc`/lint clean (same 9 pre-existing fast-refresh warnings, zero new), zero hardcoded hex.

**Likely next:** Phase 2 continues — M2.1/M2.2/M2.3 all done. **M2.4a — SkyOffice vendor & boot** (deps M2.1, done) is next on the master track: real Colyseus server vendor (`server/` → `office-realtime/`), added to docker-compose, done-when is "stock SkyOffice runs from our compose" — the client half (`web/src/office/`) is already vendored/mock-driven from session 3, this card is the SERVER half. Paste: `06 §A`, `docs/notes/skyoffice-findings.md`. Two engineering milestones also remain available independent of the phase track: **M-AR1** — Runtime extensibility proof (deps M0.5b+M0.75, both done). Paste: 13 §§3.3–3.4, 16 §5. Build: `ProcessRuntime` (or a simpler `EchoRuntime` stub if sandboxing becomes a rabbit hole) + fat-claim response (`ContextBundle` embedded directly in the claim body per 16 §5). Done-when: hire an agent with `runtimeType='echo'` → claims and completes through the pipeline unmodified; `git diff` on `routing/`/`registry/` (minus seed data) and migrations stays empty. **M-LN2** — Learning surfaces in the dashboard, frontend-only (deps M-LN1+M0.8, both done). Build: a review-queue tab on Escalations, a real "Learning" section in `EmployeeProfile` (currently a stale `EmptyState` reading "pending M-MEM1" even though M-MEM1/M-LN1 both shipped), a memory browse/inspect panel — all wired to existing 16 §3 endpoints, no new backend work. Done-when: the full reject→lesson→review→approve loop is doable in the UI with no API client needed. The Agrawal Namkeen tenant now has real `agent_stats_daily` history (Priya's live-verified "Diwali sale tagline" completion, session 22) and Rohan/Meera's real "Holi gift box" parent+2-children chain from M2.2 — good real data for M2.4c's office-avatar work or M2.6's profile stats once picked up.

Links: [[_Atrium]] · [[core-api]] · [[web-dashboard]] · [[web-office]] · [[office-realtime]]
